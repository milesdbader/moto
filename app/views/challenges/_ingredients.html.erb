<h2>Ingredients</h2>
<div class="d-flex justify-content-center">
  <div class="col-4 ingredient" >
    <i class="fas fa-fish icon" ></i>
    <h4> <%= Ingredient.all.where(category: 'protein').sample.name%> </h4>
  </div>
  <div class="col-4 offset-2 ingredient">
    <i class="fas fa-mortar-pestle icon"></i>
    <h4> <%= Ingredient.all.where(category: 'grain').sample.name%> </h4>
  </div>
</div>
<div class="d-flex justify-content-center">
  <div class="col-4 offset-2 ingredient">
    <i class="fas fa-cheese icon" ></i>
    <h4> <%= Ingredient.all.where(category: 'dairy').sample.name%> </h4>
  </div>
  <div class="col-4 offset-2 ingredient">
    <i class="fas fa-seedling icon"></i>
    <h4> <%= Ingredient.all.where(category: 'vegetable').sample.name%> </h4>
  </div>
</div>
